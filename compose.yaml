services:
  postgres:
    image: postgres:16
    container_name: cdek-task-tracker-postgres
    environment:
      POSTGRES_DB: cdekDb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Azazazru228
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
